{
  "name": "@opsboard/shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no unit tests in shell, see test:e2e\"",
    "test:e2e": "playwright test",
    "clean": "rm -rf dist coverage .turbo playwright-report test-results"
  },
  "dependencies": {
    "single-spa": "^6.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
